// Archivo innecesario: la configuración debe estar solo en postcss.config.cjs para evitar conflictos con ES modules.
module.exports = {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
}